name: evaluacion_final
description: "MelodyVault - Biblioteca Musical"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  firebase_core: ^3.12.1
  firebase_auth: ^5.5.2
  cloud_firestore: ^5.6.6
  firebase_storage: ^12.4.4
  image_picker: ^1.1.2
  file_picker: ^8.0.0+1
  cached_network_image: ^3.4.1
  google_fonts: ^6.2.1
  provider: ^6.1.2
  audioplayers: ^6.1.0
  http: ^1.6.0
  path_provider: ^2.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/images/defaults/
